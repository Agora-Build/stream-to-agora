target/
.DS_Store
*.swp
*.swo
.idea/
.vscode/
# Agora SDK staging dir (downloaded by build.rs in Phase 1)
agora-sdk/

# Agora SDK runtime artifacts (written to CWD when the binary runs)
agora_cache.db
agoradns.dat
agorareport.dat
common_resource/

# Brainstorming / planning artifacts produced by the superpowers skills.
# Kept local for the design+plan workflow; not shipped to the repo.
docs/superpowers/

# Browser-side WebRTC debugging dumps (may contain channel identifiers).
*_dump.gz
rtcstats_dump.gz
webrtc_internals_dump.gz
